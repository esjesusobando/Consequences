# 08_CTX — Session Context: Subagent Statusline + Git Fixes

> **Fecha**: 2026-05-22
> **Tema**: Documentacion plugin subagent-statusline, arreglo .gitignore, verificacion OpenCode

---

## Resumen

Sesión centrada en documentar y verificar el plugin `opencode-subagent-statusline`
de Joaquinvesapa, arreglar los paths rotos del `.gitignore` que impedían a Cursor
mostrar el estado git correctamente, y dejar todo documentado en las reglas del sistema.

---

## Plugin: opencode-subagent-statusline

| Campo | Valor |
|-------|-------|
| **Package** | `opencode-subagent-statusline` v0.7.1 |
| **Repo** | https://github.com/Joaquinvesapa/sub-agent-statusline |
| **Autor** | Joaquinvesapa (32 stars, MIT) |
| **Funcion** | Sidebar TUI que monitorea subagentes en OpenCode |
| **Estado** | ✅ Instalado y configurado |

### Dónde está instalado

```
~/.config/opencode/node_modules/opencode-subagent-statusline/
  ├── dist/tui.js      → TUI sidebar plugin (137KB)
  ├── dist/index.js    → entrypoint principal
  └── package.json     → v0.7.1
```

### Configuración en tui.json

```json
{
  "$schema": "https://opencode.ai/tui.json",
  "theme": "gentleman-kanagawa",
  "plugin": ["opencode-subagent-statusline"]
}
```

**Nota importante**: La key es `"plugin"` (singular), NO `"plugins"`.

---

## Problemas de Git Resueltos

### 1. Paths rotos en .gitignore

El `.gitignore` tenía paths del template original que no coincidían con la estructura real:

| Linea | Antes (roto) | Después (corregido) |
|-------|-------------|-------------------|
| 14 | `03_Tasks/*.md` | `03_Task/*.md` |
| 15 | `!03_Tasks/.gitkeep` | `!03_Task/.gitkeep` |
| 22 | `02_Knowledge/*.md` | `01_Personal_Os/02_Knowledge/*.md` |
| 23 | `!02_Knowledge/.gitkeep` | `!01_Personal_Os/02_Knowledge/.gitkeep` |
| 27 | `CLAUDE.md` (gitignored pero tracked — inconsistencia) | **Eliminado** |

### 2. Nested git repos sueltos

El repo clonado en `01_Repos_Reference/02_Repos_Gentleman/` tenía su propio `.git`
y no estaba ignorado. Se agregó:

```
01_Personal_Os/05_Archive/01_Repos_Reference/02_Repos_Gentleman/
```

---

## Regla de Documentación (NUEVA)

Cuando el usuario dice **"documentar"** o **"documentar todo"**:

1. Guardar resumen en `00_Context_Memory/NN_CTX_Session_YYYY-MM-DD.md`
2. Guardar detalle técnico en `01_Process_Notes/NN_NP_Descripcion_YYYY-MM-DD.md`
3. Guardar en Engram via `mem_save` + `mem_session_summary`

---

## Archivos Modificados

| Archivo | Cambio |
|---------|--------|
| `.gitignore` | Paths corregidos, nested git repos ignorados |
| `10_Git_Directions.mdc` | Sección 9 agregada, checklist expandido, fechas actualizadas |

---

*Próximo paso: Reiniciar OpenCode para activar el plugin. Si no aparece, borrar cache: `rm -rf ~/.cache/opencode/packages/`*
